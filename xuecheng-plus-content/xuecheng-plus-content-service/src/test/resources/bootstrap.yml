#微服务配置
spring:
  application:
    name: content-service
  config:
    namespace: devM
    group: project
    file-extension: yaml
    refresh-enabled: true
  profiles:
    active: dev
# 日志文件配置路径
logging:
  config: classpath:log4j2-dev.xml
